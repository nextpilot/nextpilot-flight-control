[events]
# Maximum arguments size in bytes per event.
# This can be increased if needed, but be aware that this comes at the cost of
# increased memory requirements on the autopilot side.
# The maximum is determined by the MAVLink EVENT message (40B).
max_arguments_size = 25

